# Workspace environment template
# Copy to .env, replace every CHANGE_ME value, and never commit the result.

# Application
NODE_ENV=production
APP_PORT=1234
APP_BASE_URL=http://localhost:1234
APP_TIMEZONE=Asia/Tehran
LOG_LEVEL=info

# Dashboard login. A single shared password, at least 8 characters. There is no
# user account and no registration: this value *is* the credential. Changing it
# signs out every browser immediately, because the session cookie is signed with
# a key derived from it.
AUTH_PASSWORD=CHANGE_ME_LOGIN_PASSWORD

# Notification scheduling in APP_TIMEZONE
NOTIFICATION_SEND_TIME=09:00
NOTIFICATION_POLL_INTERVAL_SECONDS=60
NOTIFICATION_MISSED_GRACE_HOURS=72
NOTIFICATION_MAX_ATTEMPTS=5

# PostgreSQL used by Docker Compose
POSTGRES_DB=reminder
POSTGRES_USER=reminder
POSTGRES_PASSWORD=CHANGE_ME_DATABASE_PASSWORD
DATABASE_URL=postgresql://reminder:CHANGE_ME_DATABASE_PASSWORD@db:5432/reminder
# Optional: Master encryption key for project secrets (if empty, falls back to DATABASE_URL)
SECRETS_MASTER_KEY=


# SMTP. Email stays unavailable until host, recipient, sender, and any
# provider-required authentication values are complete and valid.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM="Workspace <workspace@example.com>"
EMAIL_TO=

# Telegram. Leave both values empty to make Telegram unavailable in the UI.
# Create the bot with BotFather, message it once, then set the destination chat ID.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Nightly workspace backups use a separate bot and destination. When both
# values are set, the worker sends one JSON backup each night at BACKUP_SEND_TIME
# in APP_TIMEZONE. The bot must be a member/admin of the target group or channel.
TELEGRAM_BACKUP_BOT_TOKEN=
TELEGRAM_BACKUP_CHAT_ID=
BACKUP_SEND_TIME=02:00

# Optional live USD/IRR conversion for dashboard amounts.
# Get an API token at https://nerkh.io/ and send it as Authorization: Bearer.
# When empty, Settings currency is locked to DEFAULT_CURRENCY and cannot be changed.
NERKH_API_TOKEN=

# Initial setting defaults. They seed a new database only; later edits live in DB.
DEFAULT_CALENDAR_SYSTEM=jalali
DEFAULT_CURRENCY=IRR
DEFAULT_EMAIL_ENABLED=false
DEFAULT_TELEGRAM_ENABLED=false
